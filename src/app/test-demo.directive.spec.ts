import { TestDemoDirective } from './test-demo.directive';

describe('TestDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new TestDemoDirective();
    expect(directive).toBeTruthy();
  });
});
